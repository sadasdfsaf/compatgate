type Query {
  user(id: ID, includePosts: Boolean): User
  health: Health
}

type Health {
  status: String
}

type User {
  id: ID!
  email: String
  role: Role
}

enum Role {
  ADMIN
  MEMBER
  GUEST
}
